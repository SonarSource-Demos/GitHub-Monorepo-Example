using System;

namespace HelloWorld
{
    public class MyClass
    {
        public string ReturnMessage()
        {
            int await = 42;
            int async = 42;
            return "Happy coding!";
filter = "{" + request.Key + ":'" + emailAddress + "'}";
List<Customer> Donor = Providers.Donor.GetDonorByIdOrEmailAddress(filter, memberId);

string endpointCustomer = string.Format("v1.3/search?q={0}", filter);
var restContentCustomer = StoreAccess.GetRESTResponse(endpointCustomer, token);
        }
    }
}
